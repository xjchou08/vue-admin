import {
  Button,
  Input,
  MenuItem,
  Message,
  FormItem,
  Form,
  InputNumber,
} from "element-ui";

const components = [Button, Input, MenuItem, FormItem, InputNumber, Form];

const productionTipCmp = [Message];

export default {
  components,
  productionTipCmp,
};
