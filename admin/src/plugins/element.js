import {
  Button,
  Input,
  MenuItem,
  Message,
  FormItem,
  Form,
  Loading ,
} from "element-ui";

const components = [
  Button,
  Input,
  MenuItem,
  FormItem,
  Form,
];

const productionTipCmp = [Message, Loading];

export default {
  components,
  productionTipCmp,
};
